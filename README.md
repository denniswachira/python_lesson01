Rooms Flask App

This is a simple Flask web application that displays a list of rooms stored in a PostgreSQL database.
The project separates the Flask app, database connection, and configuration into different files for easy management.

